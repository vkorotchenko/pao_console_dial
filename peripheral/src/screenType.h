#ifndef SCREENTYPE_ENUM_H_
#define SCREENTYPE_ENUM_H_

class ScreenTypes {
public:

    enum ScreenType {
        PRELOAD = 0,
        CHARGE = 1,
        CAN_DATA = 2,
        SPOTIFY = 3,
        SPEEDOMETER = 4,
        SETTINGS = 5,
        GEARS = 6,
    };
};

#endif /* SCREENTYPE_ENUM_H_ */

